from base import Base_App

ib_acct = "DU" #update the ib account (different from real account and paper account)
tickers = [] #ticker list would be updated by the scannerDara wrapper function
pos_size = 3000
profit_limit = 1000
loss_limit = -500